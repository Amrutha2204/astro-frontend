# Auth API base URL (e.g. auth-service)
# Use localhost when running auth-service locally, or your ngrok/deployed URL.
NEXT_PUBLIC_API_URL=http://localhost:8001
 
# Astro / calculation API base URL (e.g. astro-service)
# Use localhost when running astro-service locally, or ngrok URL (API docs at /api-docs).
NEXT_PUBLIC_ASTRO_API_URL=https://cognisant-campanologically-maximina.ngrok-free.dev